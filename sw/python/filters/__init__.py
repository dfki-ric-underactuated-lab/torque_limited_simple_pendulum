
__all__ = ["fft", "sg"]


# cut data
    if args.clean:
        data_measured, data_desired, n_cut = data_handler.cut(data_measured, data_desired, cut_s = 930, cut_e = 1570)

    # filter data
    if args.fft:
    
    if args.sg:
